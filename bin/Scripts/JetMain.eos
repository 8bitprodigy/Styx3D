function JetMain()
{
	%Game = global.GetGameManager();
	
	%Game.EnableFrameRateCounter(1);
	%Game.SetGamma(1.0);
	
	%Game.SetDriver("Direct3D7");
	%Game.SetDriverMode(0, 0, 0);
	
	%Game.LoadWorld("Levels\tutorial2_complete.j3d");
}
